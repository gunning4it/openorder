# SPDX-License-Identifier: AGPL-3.0
# OpenOrder Dashboard - Development Dockerfile

FROM node:20-alpine

WORKDIR /app

# Install pnpm
RUN npm install -g pnpm

EXPOSE 3001

CMD ["sh", "-c", "pnpm install && cd apps/dashboard && pnpm dev"]
